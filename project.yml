name: VailMorse
options:
  bundleIdPrefix: com.jsvana
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.10"
    DEVELOPMENT_TEAM: "7UE4RDLUSX"
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: NO

targets:
  VailMorse:
    type: application
    platform: iOS
    sources:
      - Sources
    info:
      path: Info.plist
      properties:
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UIRequiredDeviceCapabilities:
          - arm64
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        CFBundleDisplayName: Vail Morse
        CFBundleShortVersionString: "1.3"
        CFBundleVersion: "4"
        # Declares the app uses only encryption exempt under US export
        # regulations (TLS via URLSession / WebSocket). Setting this here
        # makes App Store Connect skip the encryption question on every
        # release. See https://developer.apple.com/documentation/security/complying_with_encryption_export_regulations
        ITSAppUsesNonExemptEncryption: false
        # Uncomment when adding background audio:
        # UIBackgroundModes:
        #   - audio
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.jsvana.VailMorse
        TARGETED_DEVICE_FAMILY: "1,2"  # iPhone + iPad
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: NO
        SUPPORTS_MACCATALYST: NO
